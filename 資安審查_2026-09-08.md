# 資安審查報告 — tumt-wall-data(2026-09-08)

本次審查同時涵蓋 `tumt-platform` / `tumt-wall-data` / `tumt-course-115-1` 三個 repo,
本檔只記錄與 **tumt-wall-data** 有關的發現(平台後端的發現在 tumt-platform 的同名報告)。

## 方法

靜態審查(唯讀、不打外網):完整讀 `fetch.mjs`(419 行)、`test.mjs`、`package.json`、
`.github/workflows/update.yml` 與 `ci.yml`、`.gitignore`。最高嚴重度的發現經獨立審查員反駁驗證。

## 已確認的發現

### M1 — 任何人可用「別組的組名」投稿,直接覆蓋該組的頻道連結(medium)
`fetch.mjs:131-136`(讀 CSV 的 `組名` / `頻道連結` 欄)、`fetch.mjs:142-148`(`dedupeByGroup`)

```js
export function dedupeByGroup(rows) {
  const map = new Map();
  for (const row of rows) map.set(row.group, row);   // key 只有組名,後投稿覆蓋先投稿
  return [...map.values()];
}
```

組名是表單的自由填答欄位,管線沒有任何投稿者身分概念。只要知道別組組名(頻道牆上就看得到),
最後一次投稿就會蓋掉該組的頻道連結——頻道牆劫持。

建議(選一):表單改為需登入並記錄提交者、或由老師維護組名↔提交者對照後於 `fetch.mjs` 比對、
或改用「一組一列」的預先建好的試算表而非開放表單。至少在 `channels.json` 加上 `submittedAt` 讓竄改可被察覺。

## 未驗證項(有讀到程式碼,但未經反駁驗證)

- **前端 XSS 風險(medium)**:`fetch.mjs:60-70` 的 `unescapeEntities` 會把 `&lt;` 主動還原成 `<`,
  而 `group` / `channelName` / `latest[].title` / `skipped[].error` 都含外部可控字串,未經淨化即寫入
  `channels.json`(:317、:330、:349)。若前端以 `innerHTML` 渲染即為儲存型 XSS。
  前端 repo 不在本次範圍,依「不臆測」原則未升級為確認發現,但**建議優先確認前端渲染方式**。
- **`parseCsv` 先切換行再處理引號(medium)**:`fetch.mjs:107-113`,單次投稿內含換行的欄位可能被拆成多列,
  等於偽造任意資料列。
- **無列數上限造成的可用性風險(low)**:`fetch.mjs:128-137、314-321`,清單列數與欄位長度皆無上限,
  灌爆表單可讓每 5 分鐘的 Action 逾時,頻道牆停止更新。
- **`parseChannelRef` 只比對 hostname、未檢查 protocol(low)**:`fetch.mjs:153-173`,URL 白名單不完整。
- **GitHub Actions 未釘 commit SHA(low)**:`.github/workflows/update.yml:8-9、21、25、56`,
  該 job 持有可 force-push 的 `contents: write`,第三方 action 被投毒的影響面較大;`ci.yml` 同樣未釘 SHA。

## 反駁掉、不列為發現的項目

- **SSRF**:`parseChannelRef` 強制 host 必須是 youtube.com,`resolveChannelId` / `fetchRss` / `fetchLive`
  的 URL 皆為寫死的 `https://www.youtube.com` 前綴,擷取值受 `[\w.-]` 與 `UC[\w-]` 限制且不含斜線,
  無法逃逸到其他主機或做路徑穿越。
- **ReDoS**:本次未在 `parseStreamsPage` / `resolveChannelId` 找到具指數回溯特徵的 pattern。

## 未覆蓋

Google 表單本身的存取設定(是否需登入、是否限校內網域)、前端頻道牆 repo 的渲染方式、
GitHub repo 的 Actions secrets 與分支保護設定。
